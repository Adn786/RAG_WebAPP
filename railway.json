{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "backend/Dockerfile",
    "context": "backend"
  },
  "deploy": {
    "startCommand": "sh -c 'gunicorn -w 1 -b 0.0.0.0:$PORT app:app'"
  }
}